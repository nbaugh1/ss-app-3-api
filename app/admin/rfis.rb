ActiveAdmin.register Rfi do

  # See permitted parameters documentation:
  # https://github.com/activeadmin/activeadmin/blob/master/docs/2-resource-customization.md#setting-up-strong-parameters
  #
  # Uncomment all parameters which should be permitted for assignment
  #
  # permit_params :request_id, :requested_from, :nickname, :description, :suggestion, :primary_discipline, :is_urgent, :pm_contact, :status
  #
  # or
  #
  # permit_params do
  #   permitted = [:request_id, :requested_from, :nickname, :description, :suggestion, :primary_discipline, :is_urgent, :pm_contact, :status]
  #   permitted << :other if params[:action] == 'create' && current_user.admin?
  #   permitted
  # end
  
end
